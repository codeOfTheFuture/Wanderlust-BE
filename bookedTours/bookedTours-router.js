const router = require('express').Router();

const BookedTours = require('./bookedTours-model');


// Tour booked by tourist
router.add('/', async (req,res) => {
  const 
})


module.exports = router;